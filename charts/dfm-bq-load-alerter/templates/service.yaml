apiVersion: v1
kind: Service
metadata:
  name: {{ include "dfm-bq-load-alerter.fullname" . }}
  labels:
    {{- include "dfm-bq-load-alerter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dfm-bq-load-alerter.selectorLabels" . | nindent 4 }}
